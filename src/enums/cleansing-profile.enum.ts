/**
 * Named cleansing profiles for common use cases
 * Each profile contains a predefined chain of pipes
 */
export enum CleansingProfile {
  PRICE = 'price',
  PHONE = 'phone',
  EMAIL = 'email',
  DATE = 'date',
  CURRENCY = 'currency',
}
