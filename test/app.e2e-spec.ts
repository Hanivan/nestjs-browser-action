import { BrowserActionModule } from '../src/browser-action.module';

describe('BrowserActionModule (e2e)', () => {
  it('should be importable', () => {
    expect(BrowserActionModule).toBeDefined();
  });
});
